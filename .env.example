# PostgreSQL connection. Either set DATABASE_URL, or the individual PG* vars.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/leadreport

# Web server
PORT=3000

# Max upload size in MB (a 2 lakh+ row export can be large)
MAX_UPLOAD_MB=200
